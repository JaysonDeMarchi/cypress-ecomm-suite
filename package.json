{
  "name": "cypress-playground",
  "version": "1.0.0",
  "description": "Test Cypress Automation",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "open": "cypress open",
    "open:local": "cypress open --env 'name=local'",
    "open:beta8": "cypress open --env 'name=beta8'",
    "open:sqat8": "cypress open --env 'name=sqat8'"
  },
  "dependencies": {
    "cypress": "^8.2.0"
  },
  "devDependencies": {
    "@types/cypress-cucumber-preprocessor": "^4.0.1",
    "cypress-cucumber-preprocessor": "^4.3.0",
    "typescript": "^4.4.3"
  },
  "cypress-cucumber-preprocessor": {
    "nonGlobalStepDefinitions": true
  }
}
